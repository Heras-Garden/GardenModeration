# Configuration

GardenModeration depends on GardenCore and the shared database.

If Iris is used, both Iris and GardenCore must point to the same MariaDB database so remote commands and moderation events use the same integration tables.